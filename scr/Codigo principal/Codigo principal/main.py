import Wifi
import inclinacion_v2   
import mensaje_bot

#Esto es solo para que funcione la raspberry por si sola, si solo la tendrás conectada al computador (por alguna razón) puedes omitir esta parte.
